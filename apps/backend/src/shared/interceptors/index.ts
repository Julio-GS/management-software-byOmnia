/**
 * Shared Interceptors
 * 
 * Global interceptors for cross-cutting concerns:
 * - GlobalErrorInterceptor: Transforms all exceptions to structured JSON
 */

export { GlobalErrorInterceptor } from './global-error.interceptor';
