{
  "name": "@omnia/api-client",
  "version": "1.0.0",
  "description": "Unified API client for OMNIA Management System - works in both web and desktop environments",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "axios": "^1.6.7",
    "@omnia/shared-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
